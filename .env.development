# Local Vite dev server. The browser still calls /api, /game, ...
# Vite proxies those paths to this origin.
VITE_APP_API_URL=http://localhost:3000

VITE_API_PATH=/api

VITE_GAME_PATH=/game

VITE_OWNER_SERVICE_PATH=/owner-service
