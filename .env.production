# Production (Dokploy): same origin as backend nginx.
# Do not put a host here — the public IP/domain already serves /api, /game, ...
VITE_APP_API_URL=

VITE_API_PATH=/api

VITE_GAME_PATH=/game

VITE_OWNER_SERVICE_PATH=/owner-service
